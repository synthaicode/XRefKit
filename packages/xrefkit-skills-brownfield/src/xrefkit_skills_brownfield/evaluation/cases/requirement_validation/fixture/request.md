# Existing requirement

Requirement REQ-042, version 3, says that the order status is displayed in the
customer UI. The document has no owner or approval state, its update date is
unknown, and it does not define the label mapping for status code `9` or the
display when the code is unknown. Current UI evidence shows a different label
from the document. Validate the existing Requirement before planning design or
test work.
