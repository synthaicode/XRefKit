# Change request

Change a legacy .NET order calculation. Plan the work across all five phases.
The final verification must run in CI and locally, use a fixed test-data
fixture, preserve raw logs, and identify the tool and version used.
