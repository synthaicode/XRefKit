# Change request

Add a new order-status panel to an existing application. The service already
returns a status code, but the request does not define the mapping to the UI
label, the display for an unknown code, the data fixture, or the evidence to
capture. Plan the brownfield work and determine whether an AI-generated test
case can be created.
