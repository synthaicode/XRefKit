# Current specification

The order table contains `Pending`, `Approved`, and `Rejected`. The external
credit service returns an opaque decision token. No API contract or screen
specification is supplied.
