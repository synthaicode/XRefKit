# Traceability Required

Every change design item must identify the evidence basis used to decide it.

Traceability rows must connect:

- current specification source
- changed behavior or design decision
- reason for the change
- downstream implementation or test concern
- used XIDs loaded as evidence

If a source is only referenced but not loaded, it may not be listed as a used XID.
