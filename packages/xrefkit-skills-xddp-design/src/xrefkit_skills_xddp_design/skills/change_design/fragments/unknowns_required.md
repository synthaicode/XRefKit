# Unknowns Required

Unknowns are first-class output.

The Skill must list missing current specification evidence, missing decisions,
unsupported assumptions, and downstream risks caused by those gaps.

Each unknown must include:

- missing evidence or missing decision
- affected design area
- expected impact
- owner or next confirmation route when known

Do not convert unresolved unknowns into normal completion.
