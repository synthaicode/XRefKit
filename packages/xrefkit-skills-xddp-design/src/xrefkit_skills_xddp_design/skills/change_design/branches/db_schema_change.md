# DB Schema Change Branch

Use this branch when the change affects database schema, persistence structure,
or SQL-visible behavior.

Additional output should identify:

- tables, columns, constraints, or indexes affected
- migration or data correction concerns
- compatibility concerns with existing queries
- rollback or release sequencing concerns
