import fs from "node:fs/promises";
import path from "node:path";

const dir = path.resolve("human-docs/ja/assets/072_xid_usage_observability_and_improvement");
const css = await fs.readFile(path.join(dir, "diagram.css"), "utf8");
const page = (eyebrow, title, body, footer) => `<!doctype html><html lang="ja"><head><meta charset="utf-8"><style>${css}</style></head><body><main class="canvas"><div class="eyebrow">${eyebrow}</div><h1>${title}</h1>${body}<div class="footer">${footer}</div></main></body></html>`;
const cards = (items) => `<section class="grid">${items.map(([tone,title,text]) => `<article class="card ${tone}"><h2>${title}</h2><p>${text}</p></article>`).join("")}</section>`;
const slides = {
  "01_title": page("XID運用", "XID利用は、リンク維持だけでなく改善の入力になる", cards([["blue","確認すること","どの Knowledge を候補にし、解決し、読み込み、適用したか。"],["amber","見つけること","不要な文脈、足りない Knowledge、曖昧な判断境界。"],["green","改善すること","Skill、Knowledge、catalog、記録方式を evidence から直す。"]]), "AI の内面を推測せず、観測可能な記録を改善に使う。"),
  "02_states": page("状態を分ける", "resolved、loaded、applied は同じ意味ではない", `<section class="flow"><div class="node">候補 / selected<small>必要そうな XID を選ぶ</small></div><div class="arrow">→</div><div class="node amber">resolved<small>MCP が本文を返す</small></div><div class="arrow">→</div><div class="node blue">loaded<small>client がモデル文脈へ入れる</small></div><div class="arrow">→</div><div class="node green">applied<small>成果物または judgment へ結ぶ</small></div></section><div class="callout">MCP の resolved は「配信した」記録であり、loaded や applied を意味しない。</div>`, "状態を混同すると、不要な Knowledge や記録漏れの原因を誤る。"),
  "03_correlation": page("相関", "Skill Run と MCP の記録を、同じ run_id で突き合わせる", `<section class="two"><article class="panel"><h2>client 側: Skill Run log</h2><ul><li>skill_id と run_id</li><li>selected / loaded / applied XID</li><li>artifact、judgment、handoff</li></ul></article><article class="panel"><h2>MCP 側: audit log</h2><ul><li>run.bound と xid.resolved</li><li>mcp_session_id</li><li>repository_fingerprint と content hash</li></ul></article></section>`, "同じ run_id がなければ、サーバーの解決記録と作業結果を確実には結べない。"),
  "04_dashboard": page("観測", "Dashboard は、実行せずに記録を横断して確認する", `<section class="two"><article class="panel blue"><h2>Run ごとに見る</h2><div class="metric"><strong>状態</strong><span>closure、quality、unknown、handoff</span></div><div class="metric"><strong>XID</strong><span>available、selected、used、unused</span></div><div class="metric"><strong>観測</strong><span>search、load、apply、feedback</span></div></article><article class="panel green"><h2>横断して見る</h2><div class="metric"><strong>不足</strong><span>相関、検索、読み込み、適用、feedback の欠落</span></div><div class="metric"><strong>傾向</strong><span>unused XID と不足情報のランキング</span></div><div class="metric"><strong>根拠</strong><span>run log と MCP audit の対応</span></div></article></section>`, "Dashboard は正本 Knowledge でも AI 実行器でもなく、人間の観測面である。"),
  "05_resolved_not_loaded": page("診断 1", "resolved されたが loaded されない XID を確認する", cards([["amber","起きていること","MCP は本文を返したが、client はモデル文脈への読み込みを記録していない。"],["blue","確認すること","候補提示が広すぎないか。client が読み込み記録を省略していないか。"],["green","改善候補","catalog の要約・絞り込み、routing、knowledge slot、client 記録を見直す。"]]), "ただし「使わなかった」と断定しない。loaded の記録がないこと自体を扱う。"),
  "06_loaded_not_applied": page("診断 2", "loaded されたが applied の結び付きがない XID を確認する", cards([["amber","起きていること","本文はモデル文脈へ入ったが、成果物または judgment への適用先が記録されていない。"],["blue","確認すること","文脈が多すぎたか。適用先の artifact / concern 記録が足りないか。"],["green","改善候補","Knowledge の分割・要約、Skill の判断手順、apply 記録の導線を見直す。"]]), "Knowledge の価値は、存在ではなく必要な判断に結び付けられることにある。"),
  "07_missing_information": page("統制", "記録がないことを、利用しなかったという推測で埋めない", `<section class="two"><article class="panel red"><h2>してはいけない判断</h2><ul><li>audit にないから使わなかった</li><li>loaded がないから不要だった</li><li>apply がないから効果がなかった</li></ul></article><article class="panel green"><h2>正しい扱い</h2><ul><li>missing information として可視化する</li><li>MCP bind、client log、観測コマンドを確認する</li><li>必要なら Skill または運用契約を改善する</li></ul></article></section>`, "観測できないことと、起きていないことを同一視しない。"),
  "08_improvement_loop": page("改善ループ", "観測を、Skill と Knowledge の改善へ戻す", `<section class="loop"><article class="step"><b>1. 記録</b><h2>実行を相関する</h2><p>run_id と XID の状態を残す。</p></article><article class="step"><b>2. 観測</b><h2>差分を見る</h2><p>unused、not loaded、not applied、不足情報を確認する。</p></article><article class="step"><b>3. 判断</b><h2>原因を選ぶ</h2><p>routing、Skill、Knowledge、記録方式を evidence で比較する。</p></article><article class="step"><b>4. 改善</b><h2>正本を直す</h2><p>承認後に Skill、Knowledge、catalog を更新し、次の Run で再観測する。</p></article></section>`, "ログは監査の終点ではない。次の AI 作業を良くするための入力である。"),
};
await fs.mkdir(dir, { recursive: true });
for (const [name, html] of Object.entries(slides)) await fs.writeFile(path.join(dir, `${name}.html`), html, "utf8");
console.log(`rendered ${Object.keys(slides).length} XID observability slides`);
