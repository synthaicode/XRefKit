interface:
  display_name: "Brownfield Workflow"
  short_description: "Plan brownfield changes and testability"
  default_prompt: "Use $brownfield-workflow for the requested brownfield phase; carry upstream traceability forward, verify testability, generate traceable case candidates, and surface unresolved definitions explicitly."
